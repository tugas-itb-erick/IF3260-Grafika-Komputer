#include "Color.h"

static const Color WHITE = Color(255, 255, 255, 0);
static const Color BLACK = Color(0, 0, 0, 0);
static const Color RED = Color(255, 0, 0, 0);
static const Color GREEN = Color(0, 255, 0, 0);
static const Color BLUE = Color(0, 0, 255, 0);
// dst

// constructor impl..

